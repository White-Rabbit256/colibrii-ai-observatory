import PortalShell from "../../../components/portal/PortalShell";

export const metadata = {
  title: "Portal — Observatorio de IA",
  alternates: { canonical: "/app" },
};

export default function AppPage() {
  return <PortalShell />;
}
